export enum Route {
  main = '/',
  men = '/men',
  women = '/women',
  kids = '/kids',
  basket = '/basket',
  me = '/me',
  favorites = '/favorites',
  request = '/requests',
  request_create = '/requests/create',
}
